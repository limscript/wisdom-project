# 用户
type User {
  # 唯一索引
  id: ID
  # 手机号
  phone: String
  # 昵称
  name: String
  # 头像
  avatar: String
}

# 注册
input Register {
  name: String!
  phone: String!
  password: String!
  code: String!
}
